import {zodResolver} from "@hookform/resolvers/zod";
import {Controller, useForm} from "react-hook-form";
import {useEffect} from "react";
import {Button, Dialog, Select, Textarea} from "@/src/components/ui";
import {ErpStatusBadge} from "@/src/components/common";
import {creatableAftersalesTypes, type AftersalesCandidate, type AftersalesCreateFormValues, type CreatableAftersalesType} from "@/src/types/aftersales";
import {aftersalesCreateSchema} from "../aftersales.schema";

const emptyValues: AftersalesCreateFormValues = {candidateId: "", type: "维修", description: ""};

export function AftersalesCreateDialog({open, candidates, pending, error, onOpenChange, onSubmit}: {open: boolean; candidates: AftersalesCandidate[]; pending: boolean; error?: string; onOpenChange: (open: boolean) => void; onSubmit: (values: AftersalesCreateFormValues) => Promise<void>}) {
  const form = useForm<AftersalesCreateFormValues>({defaultValues: emptyValues, resolver: zodResolver(aftersalesCreateSchema), mode: "onBlur"});
  useEffect(() => {if (open) form.reset(emptyValues);}, [form, open]);
  const selected = candidates.find((item) => item.inventoryId === form.watch("candidateId"));
  return <Dialog.Root open={open} onOpenChange={(next) => {if (!pending) onOpenChange(next);}}><Dialog.Portal><Dialog.Backdrop className="fixed inset-0 erp-modal-layer bg-[var(--erp-color-backdrop)] backdrop-blur-sm" /><Dialog.Viewport className="fixed inset-0 erp-modal-layer flex items-center justify-center p-4"><Dialog.Popup className="w-full max-w-2xl rounded-[var(--erp-radius-xl)] border border-[var(--erp-color-border)] bg-[var(--erp-color-surface)] shadow-[var(--erp-shadow-popover)]"><div className="border-b border-[var(--erp-color-border)] p-5"><Dialog.Title className="text-lg font-bold">登记售后工单</Dialog.Title><Dialog.Description className="mt-1 text-sm text-[var(--erp-color-text-secondary)]">只关联已售库存卡。退货退款必须前往销售退货，避免绕过原收款分摊。</Dialog.Description></div><form onSubmit={(event) => {void form.handleSubmit(onSubmit)(event);}}><div className="space-y-5 p-5"><Field label="关联库存卡 / SN" error={form.formState.errors.candidateId?.message}><Controller control={form.control} name="candidateId" render={({field}) => <Select searchable searchPlaceholder="搜索 SN、商品、客户或销售单" value={field.value} onValueChange={field.onChange} disabled={pending} placeholder="选择已售库存卡" options={candidates.map((item) => ({value: item.inventoryId, label: `${item.serialNumber} · ${item.productName} · ${item.customerName} · ${item.saleInvoiceNo}`, disabled: Boolean(item.activeClaimId)}))} aria-label="关联库存卡" />} /></Field>{selected && <div className="grid grid-cols-2 gap-3 rounded-[var(--erp-radius-lg)] bg-[var(--erp-color-surface-muted)] p-4 text-xs"><Fact label="销售单" value={selected.saleInvoiceNo} /><Fact label="客户" value={`${selected.customerName} · ${selected.contact || "未记录联系"}`} /><Fact label="商品" value={selected.model || selected.productName} /><Fact label="SN" value={selected.serialNumber} /></div>}<Field label="售后类型" error={form.formState.errors.type?.message}><Controller control={form.control} name="type" render={({field}) => <Select value={field.value} onValueChange={(value) => field.onChange(value as CreatableAftersalesType)} options={creatableAftersalesTypes.map((value) => ({value, label: value === "换货" ? "换货咨询" : value === "补差价" ? "补差价咨询" : value}))} disabled={pending} aria-label="售后类型" />} /></Field><Field label="客户反馈" error={form.formState.errors.description?.message}><Textarea {...form.register("description")} maxLength={500} disabled={pending} className="min-h-28" placeholder="描述故障现象、客户诉求和收到实物情况" /></Field>{candidates.some((item) => item.activeClaimId) && <div className="flex items-center gap-2 text-xs text-[var(--erp-color-warning)]"><ErpStatusBadge label="防重复" tone="warning" /><span>已有处理中工单的 SN 已禁用；服务端尚无重复工单约束。</span></div>}{error && <p role="alert" className="rounded-[var(--erp-radius-md)] bg-[var(--erp-color-danger-soft)] px-3 py-2 text-xs text-[var(--erp-color-danger)]">{error}</p>}</div><div className="flex justify-end gap-2 border-t border-[var(--erp-color-border)] p-5"><Button type="button" variant="secondary" onClick={() => onOpenChange(false)} disabled={pending}>取消</Button><Button type="submit" variant="primary" disabled={pending}>{pending ? "登记中…" : "登记工单"}</Button></div></form></Dialog.Popup></Dialog.Viewport></Dialog.Portal></Dialog.Root>;
}

function Field({label, children}: {label: string; error?: string; children: React.ReactNode}) {return <label className="block text-sm font-semibold">{label}<div className="mt-2">{children}</div></label>;}
function Fact({label, value}: {label: string; value: string}) {return <div><p className="text-[var(--erp-color-text-muted)]">{label}</p><p className="mt-1 break-words font-semibold">{value}</p></div>;}
